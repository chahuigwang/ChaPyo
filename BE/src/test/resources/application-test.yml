spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-secret-key-for-ci-not-used-in-production
  access-expiration: 1800000
  refresh-expiration: 604800000